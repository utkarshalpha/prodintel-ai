# Dependencies for the ProdIntel AI Streamlit app (entrypoint: showcase/app.py).
#
# IMPORTANT: Streamlit Community Cloud resolves requirements.txt relative to the
# entrypoint's directory FIRST, so it reads THIS file (not the repo-root one).
# It must therefore list the COMPLETE runtime dependency set, mirroring
# ../requirements.txt. See that file's comments for the full rationale.

# UI
streamlit>=1.32

# Backend pulled in at import time (Live Analysis runs the real pipeline;
# importing app.api.app to break the explanation-service cycle pulls fastapi +
# anthropic, even though the deployed demo serves no HTTP and makes no Claude calls).
pydantic>=2.6,<3.0
fastapi>=0.110
sqlalchemy>=2.0
anthropic>=0.40

# File ingestion (Upload mode: CSV/TXT are stdlib; PDF/DOCX need these)
pypdf>=4
python-docx>=1.1
